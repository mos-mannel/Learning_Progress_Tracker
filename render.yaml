services:
  - type: web
    name: learning-progress-tracker
    env: python
    buildCommand: poetry install --no-dev
    startCommand: python app.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.7